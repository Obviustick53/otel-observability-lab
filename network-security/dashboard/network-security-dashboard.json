{
  "annotations": { "list": [] },
  "editable": true,
  "graphTooltip": 1,
  "panels": [
    { "id": 1, "title": "Autenticaciones fallidas (ventana de datos 5m)", "type": "stat", "gridPos": { "h": 6, "w": 6, "x": 0, "y": 0 }, "targets": [{ "expr": "sum(network_security_auth_failures_5m{environment=\"local-simulated\"})", "refId": "A" }], "fieldConfig": { "defaults": { "unit": "short" }, "overrides": [] } },
    { "id": 2, "title": "Denegaciones (ventana de datos 5m)", "type": "stat", "gridPos": { "h": 6, "w": 6, "x": 6, "y": 0 }, "targets": [{ "expr": "sum(network_security_denials_5m{environment=\"local-simulated\"})", "refId": "A" }], "fieldConfig": { "defaults": { "unit": "short" }, "overrides": [] } },
    { "id": 3, "title": "Tráfico N-S vs E-W (bytes agregados del dataset)", "type": "timeseries", "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 }, "targets": [{ "expr": "sum by (direction) (network_security_flow_bytes_total{environment=\"local-simulated\"})", "legendFormat": "{{direction}}", "refId": "A" }], "fieldConfig": { "defaults": { "unit": "bytes" }, "overrides": [] } },
    { "id": 4, "title": "Detección de tráfico anómalo", "type": "timeseries", "gridPos": { "h": 8, "w": 12, "x": 0, "y": 6 }, "targets": [{ "expr": "network_security_traffic_anomaly{environment=\"local-simulated\"}", "legendFormat": "{{signal}} / {{direction}}", "refId": "A" }], "fieldConfig": { "defaults": { "unit": "short", "min": 0, "max": 1 }, "overrides": [] } },
    { "id": 5, "title": "Observado vs baseline + 2σ", "type": "timeseries", "gridPos": { "h": 8, "w": 12, "x": 12, "y": 8 }, "targets": [{ "expr": "network_security_traffic_observed_5m{environment=\"local-simulated\"}", "legendFormat": "{{signal}} observado", "refId": "A" }, { "expr": "network_security_traffic_threshold_30m{environment=\"local-simulated\"}", "legendFormat": "{{signal}} umbral", "refId": "B" }], "fieldConfig": { "defaults": { "unit": "short" }, "overrides": [] } },
    { "id": 6, "title": "Eventos de seguridad por servicio y tipo", "type": "timeseries", "gridPos": { "h": 8, "w": 12, "x": 0, "y": 14 }, "targets": [{ "expr": "sum by (event_type, service) (network_security_events_total{environment=\"local-simulated\"})", "legendFormat": "{{event_type}} / {{service}}", "refId": "A" }], "fieldConfig": { "defaults": { "unit": "short" }, "overrides": [] } },
    { "id": 7, "title": "Security Hub — BLOQUEADO / NO EJECUTADO", "type": "text", "gridPos": { "h": 8, "w": 12, "x": 12, "y": 14 }, "options": { "mode": "markdown", "content": "La fuente local es `local-simulated`. No se generan ni se muestran findings/CVEs.\n\nSecurity Hub solo puede habilitarse después de un preflight de suscripción de la cuenta. Si la cuenta exige suscripción o la API devuelve un error de opt-in, el estado es **BLOQUEADO**. La plantilla CloudFormation queda diseñada, no ejecutada." } }
  ],
  "refresh": "15s",
  "schemaVersion": 39,
  "tags": ["security", "network", "simulated", "local", "no-securityhub-findings"],
  "time": { "from": "now-8h", "to": "now" },
  "timezone": "browser",
  "title": "OTel Lab - Network & Security (local simulated)",
  "uid": "otel-lab-security-network-source",
  "version": 2
}
